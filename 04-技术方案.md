# 04 - 技术方案

> 日期：2026-08-14（v2 修订 2026-08-15）  
> 范围：技术选型 / 系统架构 / CV 算法路线 / 开发里程碑 / 风险清单  
> v2 变更：双端并行架构（小程序引流版 + Flutter App 主力版）；iOS 平台策略抽象层；纯 JS CV 流水线方案

---

## 1. 技术选型决策

### 1.1 决策依据

MVP 阶段核心约束：
1. **2-4 周必须上线** — 技术栈必须熟悉，不能踩学习曲线
2. **双端并行** — 小程序做引流裂变 + App 做完整付费闭环
3. **CV 处理本地完成** — 不上传用户图片（隐私友好 + 省服务器钱 + 低延迟）
4. **iOS 代码层面全功能写好，平台限制作为运营问题处理** — 不为合规砍代码，运行时按平台切换策略

### 1.2 最终选型（双端并行）

| 端 | 选型 | CV 实现 | 付费能力 |
|---|---|---|---|
| **小程序引流版** | **uni-app + Vue 3 + TypeScript** | 纯 JS 实现（不依赖 OpenCV） | 微信支付组件完整写好，iOS 运行时屏蔽入口 |
| **App 主力版** | **Flutter 3 + Dart + flutter_opencv** | OpenCV C++ + Dart CIEDE2000 | iOS IAP + Google Play Billing 双端全做 |
| **色卡匹配** | **CIEDE2000 色差算法** | 业界标准，相近色表现比 RGB/Euclidean 好 30%+；TS 和 Dart 各实现一份 | - |
| **图纸生成算法** | **中值切割 + Floyd-Steinberg 抖动 + CIEDE2000 量化** | 开源项目 yourlin/Fusible-Beads-Studio 已验证这套组合，借鉴思路自己实现 | - |
| **小程序存储** | **localStorage + 微信文件系统** | 图纸/进度本地存；Stage 2 上云同步 | - |
| **App 存储** | **Isar / Hive（本地 NoSQL）** | 纯本地，无服务器；Stage 2 上云同步再加 Supabase | - |
| **App 支付** | **iOS IAP / Google Play Billing** | 双端内购，买断型 Non-consumable | - |
| **小程序支付** | **微信支付（虚拟支付组件）** | Android 端开放；iOS 端代码写好运行时屏蔽入口 | - |
| **数据统计/埋点** | **Firebase Analytics + 小程序数据助手** | 跨平台；漏斗分析 | - |
| **崩溃监控** | **Firebase Crashlytics + 小程序错误监控** | 免费；Flutter 官方支持好 | - |

### 1.3 技术选型否决项

| 被否决方案 | 否决原因 |
|---|---|
| uni-app x 单栈出小程序+App | App 端 OpenCV 集成需要 uts 原生插件，比 Flutter flutter_opencv 复杂；uni-app x 生态成熟度不足；调试链路长 |
| React Native + Expo 出 App | OpenCV 原生绑定配置地狱；团队不熟 |
| 纯 Taro + RN 出 App（Taro 4 RN 模式） | RN 跑 OpenCV 仍是原生模块集成坑；性能劣于 Flutter |
| Tauri / Web（PWA） | 摄像头能力在移动端浏览器权限体验差；App Store 上架不了，内购走不通 |
| 服务端 Python CV | 用户图必须上传 → 隐私敏感 + 服务器成本 + 网络延迟 |
| TensorFlow Lite 语义分割 | 没必要，传统 CV 足够，模型训练成本高 |
| 色卡匹配用 RGB 欧氏距离 | 相近色（奶白/米白）误判率高，必须上 LAB + CIEDE2000 |
| MPFlutter 编译 Flutter 到小程序 | 实验性项目，不成熟，风险高 |

### 1.4 为什么不"一套代码出多端"

| 考虑 | 判断 |
|---|---|
| 算法层共享 | ✅ 可以共享：CIEDE2000 色差、图纸生成抖动算法、网格采样逻辑 → 写成纯函数 TS 库，小程序直接用；App 端用 Dart 重写一份（< 500 行核心代码） |
| UI 层共享 | ❌ 不共享：小程序用 uni-app Vue 组件；App 用 Flutter Material 组件，两套体系完全不同 |
| 总体判断 | 双栈但算法复用，开发成本可接受。不为"一套代码多端"妥协 App 端 OpenCV 性能和小程序端合规 |
| 选 uni-app 而非 Taro 的理由 | 作者更熟 Vue（Harbor 前端用 Vue）；uni-app 小程序生态/教程/插件最多；Canvas 性能两框架无差异（取决于微信运行时） |

---

## 2. 系统架构（双端并行）

### 2.1 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│                    双端并行架构                                │
│                                                              │
│  ┌─────────────────────────┐    ┌─────────────────────────┐  │
│  │ 小程序引流版 (uni-app)   │    │ App 主力版 (Flutter 3)  │  │
│  │  - 微信小程序 + H5       │    │  - iOS + Android        │  │
│  │  - Vue 3 + TypeScript   │    │  - Dart + OpenCV C++    │  │
│  │  - 纯 JS CV 流水线      │    │  - flutter_opencv       │  │
│  │  - 微信支付组件(写好)    │    │  - IAP + Play Billing   │  │
│  └──────────┬──────────────┘    └──────────┬──────────────┘  │
│             │                              │                 │
│             └──────────┬───────────────────┘                 │
│                        │                                     │
│                        ▼                                     │
│           ┌──────────────────────────┐                       │
│           │  共享算法库（TS 版）     │                       │
│           │  - ciede2000.ts          │                       │
│           │  - pattern_gen.ts        │                       │
│           │  - grid_sample.ts        │                       │
│           │  - color_cards/*.json    │                       │
│           └──────────────────────────┘                       │
│                        │                                     │
│                        ▼ (Dart 移植版)                        │
│           ┌──────────────────────────┐                       │
│           │  App 端算法库 (Dart)     │                       │
│           │  - ciede2000.dart        │                       │
│           │  - pattern_gen.dart      │                       │
│           │  - grid_sample.dart      │                       │
│           │  - color_cards/*.json    │                       │
│           └──────────────────────────┘                       │
│                                                              │
│  iOS 平台策略层（platform.ts / platform.dart）                │
│  - getPaymentStrategy(): 返回当前平台的付费方式               │
│  - isPaymentAvailable(): iOS 小程序返回 false，其他 true      │
│  - getPaymentCTA(): iOS 小程序返回"下载App"，其他返回"立即购买" │
└──────────────────────────────────────────────────────────────┘
                              │
                              │  纯本地处理，无服务器依赖
                              ▼
                  （Stage 2 再引入 Supabase 云同步）
```

### 2.2 小程序版架构（uni-app + Vue 3）

```
┌────────────────────────────────────────────────────────────┐
│              微信小程序 (uni-app + Vue 3 + TS)              │
│                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐ │
│  │  图纸生成模块 │  │  免费纠错模块 │  │  引导下载App模块  │ │
│  │  (Vue UI)    │  │  (Vue UI)    │  │  (Banner+弹窗)   │ │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘ │
│         └────────┬─────────┴────────────────────┘           │
│                  │                                        │
│         ┌────────▼──────────┐                             │
│         │  纯 JS CV 流水线  │  ← 共享 TS 算法库           │
│         │  - 四角手动标定   │    (ciede2000.ts 等)        │
│         │  - Canvas 透视校正│                             │
│         │  - getImageData   │                             │
│         │  - 网格采样       │                             │
│         │  - CIEDE2000 匹配 │                             │
│         └────────┬──────────┘                             │
│                  │                                        │
│         ┌────────▼──────────┐                             │
│         │ 平台策略层        │  ← platform.ts             │
│         │  iOS: 屏蔽付费入口│                             │
│         │       显示下载App │                             │
│         │  Android: 微信支付│                             │
│         └───────────────────┘                             │
└────────────────────────────────────────────────────────────┘
```

### 2.3 App 版架构（Flutter）

```
┌────────────────────────────────────────────────────────────┐
│                    Flutter App (iOS/Android)               │
│                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐ │
│  │  图纸生成模块 │  │  分色引导模块 │  │  进度/作品存储    │ │
│  │  (Dart 纯算) │  │  (UI 状态机) │  │  (Isar 本地库)   │ │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘ │
│         │                 │                   │           │
│         └────────┬────────┴───────────────────┘           │
│                  │                                        │
│         ┌────────▼──────────┐    ┌───────────────────┐    │
│         │   UI 层 (Material) │    │  付费 / 埋点层     │    │
│         │  - 图纸画布       │    │  - StoreKit IAP   │    │
│         │  - 分色高亮层     │    │  - Play Billing   │    │
│         │  - 拍摄引导层     │    │  - Firebase Anly  │    │
│         │  - 纠错叠加层     │    │  - Crashlytics    │    │
│         │  - 实时取景AR    │    └───────────────────┘    │
│         │    (Stage 2)     │                             │
│         └────────┬──────────┘                             │
│                  │                                        │
│         ┌────────▼──────────┐                             │
│         │ OpenCV Native 层  │  ← flutter_opencv / ffi     │
│         │  - 自动四角检测   │                             │
│         │  - 透视校正       │                             │
│         │  - 网格线检测     │                             │
│         │  - 每格像素采样   │                             │
│         └────────┬──────────┘                             │
│                  │                                        │
│         ┌────────▼──────────┐                             │
│         │  CIEDE2000 匹配层 │  ← 纯 Dart 实现             │
│         │  (Dart 移植版)     │                             │
│         └───────────────────┘                             │
└────────────────────────────────────────────────────────────┘
```

### 2.4 关键数据结构（双端共享定义）

```typescript
// 色卡定义（TS 版，Dart 版同构）
interface BeadColorCard {
  brand: 'MARD' | 'Perler' | 'Hama';
  colors: BeadColor[];
}
interface BeadColor {
  code: string;          // "P-08"
  name: string;          // "奶白"
  r: number; g: number; b: number;  // sRGB
  L: number; a: number; b: number;  // LAB（预计算）
}

// 图纸
interface BeadPattern {
  id: string;
  width: number; height: number;  // 格数，如 58×58
  grid: string[];                  // 长度=w*h，存色号 code，空="."
  card: BeadColorCard;
  colorCounts: Record<string, number>;
}

// 纠错结果
interface CorrectionResult {
  takenAt: number;  // timestamp
  totalCells: number;
  mismatches: MismatchCell[];
  ambiguous: AmbiguousCell[];  // 相近色，需人工确认
}
interface MismatchCell {
  x: number; y: number;
  expectedCode: string;
  detectedCode: string;
  confidence: number;
}
```

---

## 3. CV 算法路线（双端实现）

### 3.1 算法流水线（共享逻辑）

```
输入图片（手机俯拍）
  │
  ▼
Step 1: 板区域提取（四角定位 + 透视校正）
  目标：把梯形拍摄视角 → 正方形标准板
  方法 A（自动，App 端用 OpenCV）：
    - Canny 边缘检测 → 轮廓找最大四边形 → 取四个角
    - 失败回退：findChessboardCorners 如果板上有足够对比
  方法 B（手动兜底，双端必做）：
    - 用户手动拖四个角点（屏幕上四个可拖动圆点）
    - 透视校正矩阵计算（4x4 单应矩阵，纯数学公式可实现）
  输出：warp 后的正方形板图像（像素归一化到 520×520 等）
  │
  ▼
Step 2: 网格划分
  已知规格：29×29（标准小板）、58×58（大板）、或用户自定义 N×N
  方法：在 Step 1 输出上等分 N 份得到初始网格线
  输出：每格的像素 ROI
  │
  ▼
Step 3: 每格颜色采样
  关键点：不能取整个格（珠子边缘阴影会干扰），取中心 20%-30% 区域
  方法：
    - 对每个格 ROI，取中心圆/正方形（半径=0.2*格宽）
    - 对区域内像素做中值滤波（去除噪点和反光点）
    - 取中值或均值 sRGB
  输出：w*h 个 (r,g,b)
  │
  ▼
Step 4: 色号识别（CIEDE2000 匹配）
  - sRGB → XYZ → LAB 转换（标准公式）
  - 对每格 LAB 与色卡全部 LAB 算 CIEDE2000 ΔE
  - 取 ΔE 最小的色号作为识别结果
  - 若最小 ΔE < 阈值 T1 → 高置信
    若 T1 ≤ ΔE ≤ T2 → 相近色，标记为 AmbiguousCell 需用户确认
    若 ΔE > T2 → 识别失败（该格可能是空的）
  - 阈值 T1、T2 用实测数据调
  输出：w*h 个识别色号 + 置信度
  │
  ▼
Step 5: 与图纸对比
  - 逐格 (x,y)：识别色号 vs 目标色号
  - 不一致 → 进 mismatches 列表
  - AmbiguousCell → 单独列表，UI 让用户点选
  输出：CorrectionResult
```

### 3.2 双端实现差异

| 算法步骤 | 小程序版（纯 JS） | App 版（OpenCV + Dart） |
|---|---|---|
| Step 1 自动四角检测 | ❌ 不做，只能手动 | ✅ OpenCV Canny + findContours 自动检测 |
| Step 1 手动兜底 | ✅ Canvas + 拖拽事件实现 | ✅ Flutter GestureDetector + Canvas |
| Step 1 透视校正 | ✅ 纯 JS 4x4 单应矩阵 + Canvas drawImage | ✅ OpenCV warpPerspective |
| Step 2 网格等分 | ✅ 纯 JS 数学计算 | ✅ OpenCV 或纯 Dart 计算 |
| Step 3 像素采样 | ✅ Canvas getImageData → 中值滤波 | ✅ OpenCV Mat 像素访问 |
| Step 4 CIEDE2000 | ✅ 纯 JS 实现 | ✅ Dart 实现（同算法） |
| Step 5 对比 | ✅ 纯 JS 数组操作 | ✅ Dart 数组操作 |
| **性能**（29×29 小板） | ⚠️ 3-5 秒 | ✅ <500ms |
| **性能**（58×58 大板） | ❌ 10-15 秒（需加载提示） | ✅ 1-2 秒 |
| **实时取景 AR 四角引导** | ❌ 小程序 camera 不给实时帧 | ✅ Stage 2 用 Camera plugin 实现 |

### 3.3 MVP 阶段必须做的 3 个 CV 兜底（双端一致）

| 兜底项 | 为什么 |
|---|---|
| **手动四角标定**（Step 1 方法 B） | 光线差/板边缘不完整/只拍了部分板时，自动检测必然失败，手动兜底保证核心功能可用。小程序版只走手动路径 |
| **相近色人工确认**（Step 4 AmbiguousCell） | 奶白/米白、深蓝/藏青等 ΔE 本就小，算法不可能 100% 对，交给用户确认是最稳的 |
| **单格手动修正**（结果页） | 即使算法错了，用户也能手动点某格改识别色号，不影响整体纠错结论 |

### 3.4 色卡数据准备

- 找 3 套主流色卡（MARD 256 色、Perler 120 色、Hama 100 色）
- 官方 PDF/色卡图 → 录成 JSON（code + name + sRGB 值）
- **每色卡必须自己拍一套实物标准图做 ground truth**，用官方 RGB 和实际珠子色差可能很大
- 做法：买 3 套色卡各一板，在标准白光下俯拍 → 每格中心采样 → 生成 `{brand, code, real_LAB}` 实测色卡
- 匹配时用**实测 LAB** 而不是官方 RGB，准确率会显著提升

### 3.5 小程序版图片尺寸限制策略

为保护小程序性能，引流版默认限制输入图片：
- 上传图片 ≤ 1280×1280 像素
- 拍照时引导用户对准小板（29×29 优先）
- 大板（58×58）在小程序版提示"建议下载 App 处理大板"
- 这是产品层面的引导，不是技术限制

---

## 4. iOS 平台策略抽象层（核心设计）

### 4.1 设计原则

**代码层面全功能写好，运行时按平台切换策略**。不为合规砍代码，后续政策放开或找到合规路径，改一行配置即可解锁。

### 4.2 平台策略层 API 设计

```typescript
// platform.ts - 小程序版
interface PlatformStrategy {
  isPaymentAvailable(): boolean;        // 当前平台能否走微信支付
  getPaymentCTA(): PaymentCTA;          // 付费按钮文案和行为
  getUpgradePath(): UpgradePath;         // 用户如何解锁完整功能
  getMaxImageSize(): number;             // 图片尺寸限制
  getMaxBoardSize(): number;             // 支持的最大板尺寸
}

// iOS 微信小程序运行时
const iOSWeChatStrategy: PlatformStrategy = {
  isPaymentAvailable: () => false,       // 屏蔽付费入口
  getPaymentCTA: () => ({
    text: '下载 App 解锁完整功能',
    action: 'open_app_download_page'     // 跳转 App Store
  }),
  getUpgradePath: () => 'download_app',
  getMaxImageSize: () => 1280,
  getMaxBoardSize: () => 29              // iOS 小程序限小板
};

// Android 微信小程序运行时
const AndroidWeChatStrategy: PlatformStrategy = {
  isPaymentAvailable: () => true,       // 开放微信支付
  getPaymentCTA: () => ({
    text: '¥9.9 买断无限纠错',
    action: 'wechat_pay'                 // 直接微信支付
  }),
  getUpgradePath: () => 'in_app_purchase',
  getMaxImageSize: () => 1280,
  getMaxBoardSize: () => 58
};

// 运行时检测平台
function getStrategy(): PlatformStrategy {
  const system = uni.getSystemInfoSync();
  if (system.platform === 'ios') return iOSWeChatStrategy;
  return AndroidWeChatStrategy;
}
```

### 4.3 App 端（Flutter）平台策略

```dart
// platform.dart
abstract class PlatformStrategy {
  bool get isPaymentAvailable;
  String get paymentCTAText;
  PaymentAction get paymentAction;
  int get maxImageSize;
  int get maxBoardSize;
}

// iOS App
class IOSAppStrategy implements PlatformStrategy {
  bool get isPaymentAvailable => true;
  String get paymentCTAText => '¥9.9 买断无限纠错';
  PaymentAction get paymentAction => PaymentAction.iap;
  int get maxImageSize => 4096;
  int get maxBoardSize => 100;
}

// Android App
class AndroidAppStrategy implements PlatformStrategy {
  bool get isPaymentAvailable => true;
  String get paymentCTAText => '¥9.9 买断无限纠错';
  PaymentAction get paymentAction => PaymentAction.playBilling;
  int get maxImageSize => 4096;
  int get maxBoardSize => 100;
}
```

### 4.4 各平台最终能力矩阵

| 能力 | iOS 小程序 | Android 小程序 | iOS App | Android App |
|---|---|---|---|---|
| 免费 3 次/天纠错 | ✅ | ✅ | ✅ | ✅ |
| 大板（58×58+）识别 | ❌ 限 29×29 | ⚠️ 可用但慢 | ✅ | ✅ |
| 微信支付入口 | ❌ 屏蔽 | ✅ | - | - |
| IAP / Play Billing | - | - | ✅ | ✅ |
| 引导下载 App 入口 | ✅ 显示 | ✅ 显示 | - | - |
| 自动四角检测 | ❌ 仅手动 | ❌ 仅手动 | ✅ OpenCV | ✅ OpenCV |
| 实时取景 AR 引导 | ❌ | ❌ | Stage 2 | Stage 2 |
| 分色引导 | ❌ 简化版 | ❌ 简化版 | ✅ 完整版 | ✅ 完整版 |
| 云同步（Stage 2） | ❌ | ❌ | ✅ 订阅 | ✅ 订阅 |

---

## 5. 开发里程碑（双端并行 4 周节奏）

### 5.1 拆分到周

| 周次 | App 端任务（Flutter） | 小程序端任务（uni-app） | 共享任务 | 验收标准 |
|---|---|---|---|---|
| **Week 0 准备** | Flutter 环境 + flutter_opencv POC（跑通图片灰度） | uni-app + Vue 3 + TS 项目脚手架 + 微信开发者工具配置 + uni-app POC（验证 camera/canvas/chooseImage 核心 API） | 买色卡 3 套 + 拍实物色卡 → 生成 real_LAB JSON；iOS 开发者账号续费；Android keystore 生成 | 色卡 JSON 各 ≥ 80 色；双端 HelloWorld 真机运行；OpenCV 灰度 POC 通过；uni-app 核心 API POC 通过 |
| **Week 1 图纸+付费** | 图纸生成 UI + 色卡选择 + 尺寸调整 + 生成图纸；分色引导 UI；StoreKit/Play Billing 接入买断付费 | 小程序基础框架 + 图纸生成 UI + 色卡选择 + 尺寸调整 | CIEDE2000 TS 版实现 + Dart 版移植（核心算法双端共享） | App 能生成图纸；小程序能生成图纸；沙盒内购成功 |
| **Week 2 CV 核心** | OpenCV 接入；自动四角检测 + 手动标定兜底；透视校正；网格采样；CIEDE2000 匹配；纠错结果 UI（红框+列表+相近色确认） | 纯 JS CV 流水线：手动四角标定 + Canvas 透视校正 + getImageData 网格采样 + CIEDE2000 匹配 + 纠错结果 UI | 实物色卡 LAB 数据导入双端 | 双端各跑 10×10 故意错 3 处的小板 → 全部识别 ≥ 90% |
| **Week 3 打磨+埋点+上架** | Firebase 埋点 + Crashlytics；冷启动引导；分享图生成；UI 细节；App Store Connect + Google Play 提交 | 微信支付组件接入（完整写好）；platform.ts iOS 屏蔽策略；引导下载 App 入口；小程序数据助手埋点；提交小程序审核 | 9 条小红书素材拍好备用 | App 通过审核；小程序通过审核；50 人内测无重大崩溃 |
| **Week 4 上线+裂变** | App Store / Play 正式发布 | 小程序正式发布 + 微信群卡片裂变 | 投放 9 条小红书素材（每日 1 条，薯条 100/天） | 双端可下载；群卡片可直接打开体验 |

### 5.2 关键路径依赖

```
Week 0 色卡实物拍摄 ──▶ Week 2 CIEDE2000 匹配（必须用实测 LAB）
Week 1 Store 开发者账号 ──▶ Week 3 提交审核（审核可能被拒，要留 buffer）
Week 2 CV 纠错完成 ──▶ Week 3 埋点验证（埋点事件要在真实流程里触发）
Week 0 CIEDE2000 TS 实现 ──▶ Week 2 小程序 CV 核心可直接用
Week 0 uni-app POC ──▶ Week 1 小程序图纸生成可基于验证过的框架开发
```

### 5.3 验收清单（MVP 必须通过）

**App 端（Flutter）**：
- [ ] 导入任意图片 → 3 套色卡各生成一张图纸，颜色数/分布合理
- [ ] 分色引导走完 20 色作品，当前色高亮正确，数量累加对
- [ ] 自动四角检测 + 手动标定兜底 → 拍 29×29 小板（故意摆错 5 颗）→ 全部标红，0 漏检
- [ ] 相近色（奶白 vs 米白）→ 进 Ambiguous 列表，用户确认后纠错结果更新
- [ ] 免费用户第 4 次点纠错 → 弹付费墙；沙盒内购成功后解锁无限
- [ ] 付费后冷启动重装 App → 点击「恢复购买」→ 成功恢复 Pro
- [ ] Firebase Dashboard 能看到 `first_correction_succeeded` 事件
- [ ] 模拟极端情况（暗光、糊图、只拍半板）→ App 不崩，提示"无法识别请调整角度"

**小程序端（uni-app）**：
- [ ] 微信小程序审核通过，能正常打开
- [ ] 图纸生成功能正常，能保存到本地
- [ ] 纯 JS CV 流水线跑通：29×29 小板 3-5 秒内识别完成
- [ ] iOS 微信小程序运行时：付费入口被屏蔽，显示"下载 App 解锁"按钮
- [ ] Android 微信小程序运行时：付费入口可见，沙盒微信支付成功
- [ ] 大板（58×58）上传 → 提示"建议下载 App 处理大板"
- [ ] 群卡片分享 → 群友点开 → 能直接用 3 次免费纠错
- [ ] 引导下载 App 入口跳转正确（iOS 跳 App Store，Android 跳应用市场）

---

## 6. 风险清单 & 应对

| 风险 | 概率 | 影响 | 应对预案 |
|---|---|---|---|
| **CV 准确率不达标（< 80%）** | 中 | 致命：用户信任丧失 | 优先上手动兜底 + 相近色确认；用自己买的实物板反复测；不行就外包给 CV 朋友做一周咨询 |
| **App Store 审核被拒（内购/功能）** | 中高 | 延迟上线 | 提前看审核指南 3.1.1（内购）、5.1（相机权限）；留 1 周 buffer；不行先上 TestFlight 灰度 |
| **微信小程序审核被拒（虚拟付费）** | 中 | 小程序端延迟上线 | 代码层面 iOS 已屏蔽付费，Android 走虚拟支付组件合规路径；如审核卡，先上纯免费版，付费后续补 |
| **flutter_opencv 绑定坑多** | 中 | App 开发延期 | 先找最活跃的 fork 做 POC（1 天），不行换 `opencv_dart` 或直接 `flutter_ffigen` 自己绑；极端情况 App 端也走纯 JS CV |
| **小程序 Canvas getImageData 性能瓶颈** | 高 | 大板识别慢 | 限制图片尺寸 ≤ 1280×1280；大板引导下载 App；分块处理（先识别 1/4 板，再拼接） |
| **色卡官方 RGB 与实物色差大** | 高 | 准确率下降 | Week 0 必做：买色卡自己拍实物做 ground truth，不用官方值 |
| **Android 机型碎片化（相机崩溃）** | 高 | 差评 | 用 `camera` 官方插件；在 3 台主流机（红米/荣耀/三星）上必测 |
| **首次启动冷启动流失高（> 60%）** | 中 | 日活上不去 | 引导页必须 3 步内完成，放真实震撼 demo GIF；首次引导直接完成一次"示例图+示例板"纠错体验 |
| **iOS 微信支付政策变动** | 低 | iOS 小程序付费路径 | 代码已写好，政策放开改一行配置解锁；当前 iOS 走"引导下载 App"路径兜底 |
| **uni-app + Vue 3 踩坑** | 中 | 小程序开发延期 | Week 0 做 POC 验证核心 API（camera + canvas + chooseImage）；uni-app 文档/教程最多，社区活跃，风险低；不行回退到原生小程序开发 |
